{
  "name": "functions",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "echo no lint",
    "build": "echo no build step",
    "deploy": "firebase deploy --only functions:sendDailyReminders",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.2",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.4.1"
  }
}

